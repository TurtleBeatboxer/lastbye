import { RecoveryQuestionFormComponent } from './recovery-question-form.component';

describe('RecoveryQuestionFormComponent', () => {
  it('should mount', () => {
    cy.mount(RecoveryQuestionFormComponent);
  });
});
